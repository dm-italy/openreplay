defaultNamespace: openreplay
helm:
  releaseName: databases
  valuesFiles:
    - values-fleet.yaml
  values:
    postgresql:
      enabled: false
    clickhouse:
      enabled: false
    kafka:
      enabled: false
    vault:
      enabled: false
